// import { OrderMessage } from "./types/order-message";

export type Task = {
  id: string;
  title: string;
  completed: boolean;
};

export type User = {
  id: string;
  name: string;
};

// export { OrderMessage };
